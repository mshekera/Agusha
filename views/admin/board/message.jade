extends layout

block content
	div(class= (success==true?".panel.panel-success":failure==true?".panel.panel-danger":".panel.panel-default"))
		.panel-heading
			if success
				.panel-title= "Операция выполнена успешно"
			else
				.panel-title= "Операция выполнена с ошибкой"
		.panel-body= message